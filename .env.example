# Database
DATABASE_URL=postgresql://pflanzenulli:pflanzenulli_dev@localhost:5432/pflanzenulli
POSTGRES_PASSWORD=pflanzenulli_dev

# Redis
REDIS_URL=redis://localhost:6379

# Meilisearch
MEILISEARCH_URL=http://localhost:7700
MEILI_MASTER_KEY=meilisearch_dev_key

# Minio (S3-compatible object storage)
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_BUCKET=pflanzenulli
MINIO_USE_SSL=false

# Auth.js
AUTH_SECRET=generate-a-random-secret-here
AUTH_URL=http://localhost:3000

# OAuth Providers (configure before use)
# AUTH_GOOGLE_ID=
# AUTH_GOOGLE_SECRET=
# AUTH_APPLE_ID=
# AUTH_APPLE_SECRET=

# Email (SMTP)
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_SECURE=false
# SMTP_FROM=Pflanzenulli <noreply@your-domain.eu>

# Subscription billing (Paddle)
# PADDLE_API_KEY=
# PADDLE_WEBHOOK_SECRET=

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# Feature flags
EARLY_ACCESS_ENABLED=true
